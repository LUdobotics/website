# Cloud staging

See [environment setup](environments.md) for the planned preproduction website, API, Clerk application, and release flow.

The old `cloud-staging.ludobotics.com` example is not a verified public API endpoint. Configure and validate the staging API hostname and TLS certificate before building a preproduction website against it.
